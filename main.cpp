# include "Menu.h"

// #include "ProjectPaths.h"
// #include <iostream>

int main()
{

  // std::cout << ProjectPaths::get("Attendance/PSO-101.json") << std::endl; 

  // ifstream inFile(ProjectPaths::get("Attendance/PSO-101.json"), ios::in); 

  //  try {
    
  //   if (!inFile.is_open())
  //     throw std::runtime_error{"Could not open the attendance file !"};

  // } catch (exception& ex) { 
  //   std::cerr << "Exception: " << ex.what() << std::endl;
  // }


  Menu();
  return 0;
}
